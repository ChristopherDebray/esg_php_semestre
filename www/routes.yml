default:
  controller: "PageController"
  action: "listPages"

contact:
  controller: "Front"
  action: "contact"
  permission: ["admin"]

se-connecter:
  controller: "Security"
  action: "login"

s-inscrire:
  controller: "Security"
  action: "register"

login:
  controller: "Security"
  action: "login"

confirm-user-inscription:
  controller: "Security"
  action: "confirmUserEmail"

deconnexion:
  controller: "Security"
  action: "logout"

dashboard:
  controller: "Admin"
  action: "getUsers"
  permission: ["admin"]

dashboard-pages:
  controller: "Admin"
  action: "getPages"
  permission: ["admin"]

dashboard-reportings:
  controller: "Admin"
  action: "getReportings"
  permission: ["admin"]

switch-page-status:
  controller: "Admin"
  action: "switchPageStatus"
  permission: ["admin"]

hard-delete-user:
  controller: "Admin"
  action: "hardDeleteUser"
  permission: ["admin"]

soft-delete-user:
  controller: "Admin"
  action: "softDeleteUser"
  permission: ["suscriber"]

update-user:
  controller: "Admin"
  action: "updateUser"
  permission: ["admin"]

wireframe1:
  controller: "DesignTestController"
  action: "displayWireframe1"

wireframe2:
  controller: "DesignTestController"
  action: "displayWireframe2"

wireframe3:
  controller: "DesignTestController"
  action: "displayWireframe3"

create-page:
  controller: "PageController"
  action: "create"
  permission: ["admin"]

update-page:
  controller: "PageController"
  action: "update"
  permission: ["admin"]

validate-comment:
  controller: "Admin"
  action: "validateComment"
  permission: ["admin"]

block-comment:
  controller: "Admin"
  action: "blockComment"
  permission: ["admin"]

signal-comment:
  controller: "PageController"
  action: "signalComment"

pages-list:
  controller: "PageController"
  action: "listPages"

components-documentation:
  controller: "Admin"
  action: "componentDocumentation"

# route for deployement CMS

deploy:
  controller: "Deploy"
  action: "deployDb"

deploy/user:
  controller: "Deploy"
  action: "deployUser"

sitemap.xml:
  controller: "Front"
  action: "sitemap"
